const state = {
    hello: 'Hello use Vuex',
};

const getters = {
    getHello(state) {
        return state.hello;
    },
};

const actions = {
};

const mutations = {
};

export default {
    state,
    getters,
    actions,
    mutations,
};
